global:
  scrape_interval: 15s

scrape_configs:
  # This is telling prometheus to scrape itself
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']
  # Add more jobs here
  